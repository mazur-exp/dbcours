<div class="min-h-screen bg-gray-50" data-controller="kanban" data-kanban-update-url-value="<%= crm_update_user_status_path(':id') %>">
  <!-- Верхняя навигация -->
  <nav class="bg-white shadow-sm border-b border-gray-200 sticky top-0 z-10">
    <div class="max-w-full mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between h-16">
        <div class="flex items-center">
          <div class="flex-shrink-0 flex items-center">
            <%= link_to root_path, class: "text-xl md:text-2xl font-bold text-green-600" do %>
              <span class="hidden sm:inline">📚 Bali Food Delivery Master</span>
              <span class="sm:hidden">📚 BFDM</span>
            <% end %>
            <span class="ml-4 text-gray-400">|</span>
            <span class="ml-4 text-lg font-semibold text-gray-700">CRM</span>
          </div>
        </div>
        <div class="flex items-center gap-4">
          <!-- Статистика -->
          <div class="hidden md:flex items-center gap-6 text-sm">
            <div class="flex items-center gap-2">
              <span class="text-gray-500">Всего:</span>
              <span class="font-bold text-gray-900"><%= @stats[:total] %></span>
            </div>
            <div class="flex items-center gap-2">
              <span class="text-gray-500">Горячие:</span>
              <span class="font-bold text-red-600">🔥 <%= @stats[:hot_leads] %></span>
            </div>
            <div class="flex items-center gap-2">
              <span class="text-gray-500">Оплатили:</span>
              <span class="font-bold text-green-600"><%= @stats[:paid] %></span>
            </div>
          </div>
          <%= render 'shared/auth_button' %>
        </div>
      </div>
    </div>
  </nav>

  <!-- Канбан-доска -->
  <div class="p-4 overflow-x-auto">
    <div class="inline-flex gap-4 min-w-full">
      <% User.crm_statuses.keys.each do |status| %>
        <%= render 'crm/dashboard/kanban_column', status: status, users: @users_by_status[status] %>
      <% end %>
    </div>
  </div>
</div>
