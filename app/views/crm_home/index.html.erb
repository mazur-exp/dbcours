<div class="min-h-screen bg-gradient-to-br from-gray-50 via-white to-blue-50 flex items-center justify-center px-4">
  <div class="max-w-md w-full">
    <!-- Навигация -->
    <div class="absolute top-4 right-4">
      <%= render 'shared/auth_button' %>
    </div>

    <!-- Контент -->
    <div class="bg-white rounded-2xl shadow-xl p-8 text-center">
      <!-- Иконка -->
      <div class="mb-6">
        <div class="inline-flex items-center justify-center w-20 h-20 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-full">
          <svg class="w-10 h-10 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
          </svg>
        </div>
      </div>

      <!-- Заголовок -->
      <h1 class="text-3xl font-bold text-gray-900 mb-4">
        AI Delivery CRM
      </h1>

      <% if @current_user %>
        <!-- Авторизован, но не админ -->
        <div class="bg-red-50 border border-red-200 rounded-lg p-6 mb-6">
          <p class="text-red-800 font-semibold mb-2">🔒 Доступ запрещен</p>
          <p class="text-red-600 text-sm">
            Эта страница доступна только для администраторов.
          </p>
        </div>

        <%= link_to "https://course.aidelivery.tech", class: "inline-block bg-gradient-to-r from-blue-500 to-indigo-600 text-white font-semibold py-3 px-8 rounded-lg hover:from-blue-600 hover:to-indigo-700 transition-all shadow-lg" do %>
          ← Вернуться к курсу
        <% end %>
      <% else %>
        <!-- Не авторизован -->
        <p class="text-gray-600 mb-8">
          Система управления лидами и мессенджер<br>
          для менеджеров AI Delivery
        </p>

        <div class="bg-blue-50 border border-blue-200 rounded-lg p-6 mb-6">
          <p class="text-blue-800 font-semibold mb-2">👋 Добро пожаловать!</p>
          <p class="text-blue-600 text-sm">
            Для доступа к CRM необходимо авторизоваться через Telegram
          </p>
        </div>

        <!-- Кнопка авторизации будет из _auth_button partial -->
        <div class="flex flex-col gap-4">
          <div id="auth-container" class="flex justify-center"></div>

          <p class="text-xs text-gray-500 mt-4">
            Доступ только для администраторов
          </p>
        </div>
      <% end %>
    </div>

    <!-- Дополнительная информация -->
    <div class="mt-8 text-center text-sm text-gray-500">
      <p>AI Delivery Platform</p>
      <p class="mt-2">
        <a href="https://course.aidelivery.tech" class="text-blue-600 hover:underline">Курс</a>
        <span class="mx-2">•</span>
        <a href="https://tracker.aidelivery.tech" class="text-blue-600 hover:underline">Трекер</a>
      </p>
    </div>
  </div>
</div>

<!-- Скрипт авторизации -->
<%= render 'shared/auth_script' %>
