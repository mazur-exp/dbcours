<div class="min-h-screen bg-gray-50">
  <!-- Навигация -->
  <nav class="bg-white shadow-sm border-b border-gray-200">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between h-16">
        <div class="flex items-center gap-4">
          <%= link_to root_path, class: "text-xl font-bold text-green-600" do %>
            📚 BFDM
          <% end %>
          <span class="text-gray-400">|</span>
          <span class="text-lg font-semibold text-gray-700">Источники трафика</span>
        </div>
        <div class="flex items-center gap-4">
          <%= link_to crm_path, class: "text-sm text-gray-600 hover:text-gray-900" do %>
            ← Вернуться в CRM
          <% end %>
          <%= render 'shared/auth_button' %>
        </div>
      </div>
    </div>
  </nav>

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <!-- Статистика -->
    <div class="grid grid-cols-1 md:grid-cols-4 gap-4 mb-8">
      <div class="bg-white rounded-lg shadow p-6">
        <div class="text-sm text-gray-500">Всего кликов</div>
        <div class="text-2xl font-bold text-gray-900"><%= @total_stats[:total_clicks] %></div>
      </div>
      <div class="bg-white rounded-lg shadow p-6">
        <div class="text-sm text-gray-500">Всего лидов</div>
        <div class="text-2xl font-bold text-blue-600"><%= @total_stats[:total_leads] %></div>
      </div>
      <div class="bg-white rounded-lg shadow p-6">
        <div class="text-sm text-gray-500">Конверсий</div>
        <div class="text-2xl font-bold text-green-600"><%= @total_stats[:total_conversions] %></div>
      </div>
      <div class="bg-white rounded-lg shadow p-6">
        <div class="text-sm text-gray-500">Конверсия</div>
        <div class="text-2xl font-bold text-purple-600"><%= @total_stats[:overall_conversion_rate] %>%</div>
      </div>
    </div>

    <!-- Кнопка создать -->
    <div class="mb-6">
      <%= link_to new_traffic_source_path, class: "inline-flex items-center gap-2 bg-green-500 hover:bg-green-600 text-white font-semibold py-3 px-6 rounded-lg transition-colors" do %>
        <span>➕</span>
        <span>Создать источник</span>
      <% end %>
    </div>

    <!-- Таблица источников -->
    <div class="bg-white rounded-lg shadow overflow-hidden">
      <table class="min-w-full divide-y divide-gray-200">
        <thead class="bg-gray-50">
          <tr>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Название</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Продукт</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Тип</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Короткая ссылка</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Клики</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Лиды</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Оплатили</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Конверсия</th>
            <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase">Действия</th>
          </tr>
        </thead>
        <tbody class="bg-white divide-y divide-gray-200">
          <% @traffic_sources.each do |source| %>
            <tr class="hover:bg-gray-50">
              <td class="px-6 py-4">
                <div class="font-medium text-gray-900"><%= source.name %></div>
                <div class="text-xs text-gray-500"><%= source.utm_source %> / <%= source.utm_medium %></div>
              </td>
              <td class="px-6 py-4">
                <span class="px-2 py-1 text-xs font-semibold rounded-full <%=
                  case source.product
                  when 'course' then 'bg-green-100 text-green-800'
                  when 'tracker' then 'bg-orange-100 text-orange-800'
                  when 'consulting' then 'bg-purple-100 text-purple-800'
                  else 'bg-gray-100 text-gray-800'
                  end %>">
                  <%= source.product_display_name %>
                </span>
              </td>
              <td class="px-6 py-4">
                <span class="px-2 py-1 text-xs font-semibold rounded-full <%= source.site? ? 'bg-blue-100 text-blue-800' : 'bg-purple-100 text-purple-800' %>">
                  <%= source.site? ? '🌐 Сайт' : '🤖 Бот' %>
                </span>
              </td>
              <td class="px-6 py-4">
                <div class="flex items-center gap-2">
                  <code class="text-xs bg-gray-100 px-2 py-1 rounded"><%= source.short_url %></code>
                  <button onclick="navigator.clipboard.writeText('<%= source.short_url %>')" class="text-green-600 hover:text-green-700">
                    📋
                  </button>
                </div>
              </td>
              <td class="px-6 py-4 text-sm text-gray-900"><%= source.clicks_count %></td>
              <td class="px-6 py-4 text-sm font-semibold text-blue-600"><%= source.leads_count %></td>
              <td class="px-6 py-4 text-sm font-semibold text-green-600"><%= source.conversions_count %></td>
              <td class="px-6 py-4 text-sm font-semibold text-purple-600"><%= source.conversion_rate %>%</td>
              <td class="px-6 py-4 text-right text-sm font-medium">
                <div class="flex items-center justify-end gap-2">
                  <%= link_to traffic_source_path(source), class: "text-blue-600 hover:text-blue-700" do %>📊<% end %>
                  <%= link_to edit_traffic_source_path(source), class: "text-gray-600 hover:text-gray-700" do %>✏️<% end %>
                  <%= button_to traffic_source_path(source), method: :delete, data: { turbo_confirm: "Удалить источник?" }, class: "text-red-600 hover:text-red-700" do %>🗑️<% end %>
                </div>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>

      <% if @traffic_sources.empty? %>
        <div class="text-center py-12">
          <span class="text-4xl">📊</span>
          <p class="mt-4 text-gray-500">Нет источников трафика. Создайте первый!</p>
        </div>
      <% end %>
    </div>
  </div>
</div>
